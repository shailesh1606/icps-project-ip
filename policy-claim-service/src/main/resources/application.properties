server.port=${PORT:8082}
spring.data.mongodb.uri=${MONGODB_URI}
spring.data.mongodb.database=policydb

jwt.secret=${JWT_SECRET}
jwt.expiry=${JWT_EXPIRY:86400000}

# Payment service base URL (can be overridden by PAYMENT_SERVICE_URL env var)
payment.service.url=${PAYMENT_SERVICE_URL:http://localhost:8083/payments/create}
